 Buenas a todos soy Adrián y vamos a ver cómo podemos hacer fetch en un proyecto de react lo que queremos es hacer una petición a un api para mostrarla en nuestra página lo que tenemos de momento es una aplicación muy sencillita con el componente raíz que tienen un difundi y nada más y donde queremos hacer una petición es a una api gratuita que estamos utilizando la la y es un placeholder pero podríamos utilizar cualquier otra para obtener unos datos de usuario que como vemos aquí son una rai de objetos de usuarios bueno pues como podemos hacer esto para hacer un fetch lo que necesitamos es guardar estos datos en una variable así que lo primero que tenemos que hacer es crear esta variable que va a almacenar los datos y como esta variable se va a modificar porque va a empezar vacía y después se va a rellenar vamos a creer una variable de estado creamos aquí una variable vamos a llamarla como queramos users por ejemplo y también ponemos sus set usas y esto va a ser igual a us state y el valor inicial que va a ser una rai vacío vale ya tenemos esta variable que más podemos hacer pues ahora necesitamos una función para hacer el fetch vamos a creer una función vamos a llamarla por ejemplo get usas y esto va a ser una función que como va a hacer un fetch como va a hacer un fetch va a ser así en crona si que necesitamos escribir aquí así para crear una función así en crona y cada vez que hacemos un fetch obtenemos una respuesta si vamos a crear una una variable llamamos la como queramos pero habitualmente se llama response que va a ser igual a fetch a donde queramos y como fetch es así en crona tenemos que esperar así que a wait fetch y aquí entre comillas ponemos la URL del API copiamos pegamos aquí lo tenemos vale pues ya estamos haciendo el fetch bueno todavía no porque no estamos ejecutando esta función pero cuando se ejecute ya se estaría haciendo el fetch ahora necesitamos transformar esta respuesta para obtener los datos vamos a creer una variable que llamaremos data y esto va a ser igual a response punto Jason estoy hacerlo siempre cuando cuando utilizamos el fetch y esto también es así en crona con lo que también tenemos que esperar a wait response punto Jason y ya tenemos nuestros datos y estos datos son los que queremos para nuestros usuarios así que lo que vamos a hacer aquí una vez se termine todo es se tear los usuarios a los datos perfecto bueno y ahora que tenemos ya esta función y que tenemos nuestra variable rellena como ejecutamos o cuando ejecutamos esta función bueno pues ya sabemos que podemos utilizar un hook el use effect para que ejecutes cierto código cuando el proyecto bueno cuando el componente se monta así que vamos a creer aquí un use effect que recordemos recibe un callback como primer argumento que será lo que se ejecute y como segundo argumento recibe unos corchetes vacíos y queremos que esto se ejecute cuando se monta si no ponemos este segundo argumento lo que haya en el callback se ejecutará cada vez que el componente recibe algún tipo de cambio y si hacemos una petición un API no queremos eso queremos simplemente llamarla una vez obtener los datos y ya está así que únicamente la vamos a llamar cuando se monte el componente y aquí dentro del use effect pues llamamos a gate us usas que como es un método le ponemos paréntesis ya está nada más se monte el componente se llama gate usas gate usas hace la petición hace hace el fecha lápí se tea la variable estado usas y ahora ya podemos utilizar usas para pintar los datos en pantalla y que es lo que vamos a hacer vamos a lápí vamos a ver la estructura vemos que cada usuario tiene un ID que nos puede servir como aquí y tiene un 9 que va a ser lo que vamos a pintar podríamos pintar los demás propiedades pero bueno vamos a quedarnos solamente con esa porque la idea aquí es saber hacer un fetch y el resto es ya trivial bueno pues es lo que podemos hacer como esto es una raíz de objetos vamos a transformarlo en algo que luego podamos pintar nuestro html por ejemplo aquí voy a crear una nueva variable la voy a llamar html usas por darle algún nombre y esto va a ser igual a usas es decir la variable estado punto map con lo que vamos a obtener un nuevo array y aquí vamos a decir que por cada user dentro de usas devolvamos retorno algún contenido o alguna estructura html es encigita para no complicarnos podemos volverlo que queramos vamos a devolver un div por ejemplo y dentro del div pues vamos a tener un h3 que tenga el user punto 9 bueno aquí tenemos el user punto 9 pero podríamos seguir añadiendo elementos para pintar todo lo demás y como esto es una rai necesita un key así que le pasamos aquí la clave que va a ser user punto ID perfecto pues ya tenemos nuestro nuevo array de elementos html que podemos pintar en nuestra página así que vamos a ponernos aquí por ejemplo vamos a crear una sección y dentro de sección vamos a pintar pues todos los usuarios html usas y aquí los tenemos con lo que ya habríamos terminado así que como vemos no están complicados se hacen unos o en unos simples pasos vamos a repasar lo que hemos hecho hemos creado un componente ya sea el componente raíz o cualquier otro y hemos creado una función perdón una variable que va a almacenar la información del app esta variable necesita ser del state porque cada vez que haya un cambio queremos que eso se se muestra en la página como rellenamos esta variable bueno pues creamos una función donde hacemos un fetch normal y corriente como haríamos en JavaScript y cuando obtengamos los datos simplemente hacemos set en este caso es el ser de la variable y le pasamos los datos ahora ya la variable user contiene toda la información cuando llamamos a get users pues nada más se monte el componente usando user fake y después transformamos users bueno no es que transformemos sino que lo utilizamos para crear una nueva variable que que tenga el mismo número de elementos es decir una raíz pero con elementos html y ahora ya está variable podemos mostrarla en nuestra página así que como vemos a hacer un fetch no es nada complicado en react es muy parecido a hacerlo con JavaScript espero que esto se ha deshidote y hasta la próxima